/**
 * The category of game being played.
 * Each type maps to a distinct UI and question format on the frontend.
 * IMAGE and WORD are reserved for future game types.
 */
package cephadex.brainflex.model.enums;

public enum GameType {
    TRIVIA,
    IMAGE,
    WORD
}
