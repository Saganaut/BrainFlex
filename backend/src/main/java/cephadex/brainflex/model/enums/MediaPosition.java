/**
 * Where a question or slide renders its attached media (image / video / audio).
 *   TOP        — above the prompt
 *   BOTTOM     — below the prompt
 *   BACKGROUND — full-bleed background, prompt overlays it
 *   NONE       — element carries no media
 */
package cephadex.brainflex.model.enums;

public enum MediaPosition {
    TOP,
    BOTTOM,
    BACKGROUND,
    NONE
}
