/**
 * Layout / styling variant for a Slide. The frontend renderer uses this hint to
 * pick typography, alignment, and accent visuals.
 */
package cephadex.brainflex.model.enums;

public enum SlideKind {
    TITLE,
    SECTION,
    CALLOUT,
    CONTENT,
    END
}
